using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class mapgen : MonoBehaviour
{
    //node game object
    //spawn nodes in a list at random locations with random events assigned
    //make left most node the entrance to sector and the left most node the exit
    //change color to green of all nodes withing range of current node
    //allow jumping to nodes of green and not to nodes of red, current node is blue, entrance node is purple, exit node is yellow
    //


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
